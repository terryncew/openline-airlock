# RIL-ANOMALY-002 operator card

The receiver keeps the arm mapping private. You only copy the packet/prompt you are given.

For each arm:
1. Open a brand-new Gemini **Temporary Chat** using the same qualified **Flash Extended** selection.
2. Confirm the Temporary Chat indicator is visible before the first packet.
3. Keep that same Temporary Chat open for all four rounds of that arm.
4. Paste only the receiver-supplied protocol + current packet. Return Gemini's exact JSON response.
5. Do not paste anything from the other arm.
6. After round 4, close the Temporary Chat. The next arm starts in a different fresh Temporary Chat.

Do not manually summarize history. The receiver generates each next packet with only that arm's permitted history.
