{
 "meta": {
  "apparatus_limit": "generator space 815 distinct; 17 fully-fresh available",
  "n_tasks": 465,
  "rule": "discovery+promotion disjoint from prior ECON and pb2-contacted; operating disjoint from pb2-contacted (apparatus-limit adaptation, declared pre-contact)",
  "seeds": [
   20260926
  ],
  "study": "PAYBACK-003"
 },
 "tasks": [
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0000",
   "template": "qty_plus_price+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0001",
   "template": "bulk_rebate_threshold+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0002",
   "template": "qty_plus_price+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0003",
   "template": "refund_includes_tax+qty_plus_price+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0004",
   "template": "bulk_rebate_threshold+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0005",
   "template": "bulk_rebate_threshold+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0006",
   "template": "refund_includes_tax+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0007",
   "template": "bulk_rebate_threshold+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0008",
   "template": "tax_on_prediscount+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0009",
   "template": "tax_on_prediscount+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0010",
   "template": "tax_on_prediscount+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0011",
   "template": "tax_on_prediscount+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0012",
   "template": "refund_includes_tax+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0013",
   "template": "qty_plus_price+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0014",
   "template": "refund_includes_tax+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0015",
   "template": "once_shared_holder+count_off_by_one+emit_skips_first"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0016",
   "template": "boundary_exclusive+set_no_mkdir+merge_mutates_base"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0017",
   "template": "emit_skips_first+unsubscribe_wrong_key+count_off_by_one"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0018",
   "template": "shallow_merge+boundary_exclusive+set_no_mkdir"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0019",
   "template": "unsubscribe_wrong_key+count_off_by_one+once_never_removes"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0020",
   "template": "shallow_merge+precedence_flipped+merge_mutates_base"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0021",
   "template": "tax_on_prediscount+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0022",
   "template": "precedence_flipped+shallow_merge+snapshot_shallow"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0023",
   "template": "qty_plus_price+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0024",
   "template": "unsubscribe_wrong_key+map_emit_reversed+emit_stops_on_exception"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0025",
   "template": "boundary_exclusive+set_no_mkdir+shallow_merge"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0026",
   "template": "qty_plus_price+bulk_rebate_threshold+tax_on_prediscount"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0027",
   "template": "map_emit_reversed+emit_stops_on_exception+once_shared_holder"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0028",
   "template": "set_no_mkdir+merge_mutates_base+shallow_merge"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0029",
   "template": "qty_plus_price+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0030",
   "template": "once_never_removes+unsubscribe_wrong_key+emit_skips_first"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0031",
   "template": "merge_mutates_base+shallow_merge+boundary_exclusive"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0032",
   "template": "once_never_removes+unsubscribe_wrong_key+once_shared_holder"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0033",
   "template": "shallow_merge+precedence_flipped+merge_mutates_base"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0034",
   "template": "tax_on_prediscount+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0035",
   "template": "once_never_removes+emit_skips_first+emit_stops_on_exception"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0036",
   "template": "merge_mutates_base+precedence_flipped+boundary_exclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0037",
   "template": "tax_per_mille+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0038",
   "template": "unsubscribe_wrong_key+map_emit_reversed+count_off_by_one"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0039",
   "template": "tax_on_prediscount+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0040",
   "template": "count_off_by_one+once_shared_holder+unsubscribe_wrong_key"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0041",
   "template": "qty_plus_price+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0042",
   "template": "qty_plus_price+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0043",
   "template": "set_no_mkdir+precedence_flipped+merge_mutates_base"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0044",
   "template": "refund_includes_tax+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0045",
   "template": "boundary_exclusive+precedence_flipped+shallow_merge"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0046",
   "template": "threshold_inclusive+tax_per_mille+bulk_rebate_threshold"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0047",
   "template": "count_off_by_one+once_never_removes+emit_skips_first"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0048",
   "template": "boundary_exclusive+shallow_merge+merge_mutates_base"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0049",
   "template": "map_emit_reversed+unsubscribe_wrong_key+once_never_removes"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0050",
   "template": "bulk_rebate_threshold+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0051",
   "template": "emit_skips_first+emit_stops_on_exception+once_shared_holder"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0052",
   "template": "boundary_exclusive+merge_mutates_base+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0053",
   "template": "refund_includes_tax+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0054",
   "template": "boundary_exclusive+shallow_merge+snapshot_shallow"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0055",
   "template": "once_never_removes+count_off_by_one+map_emit_reversed"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0056",
   "template": "once_shared_holder+map_emit_reversed+emit_skips_first"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0057",
   "template": "shallow_merge+precedence_flipped+snapshot_shallow"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 < v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0058",
   "template": "boundary_exclusive+shallow_merge+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0059",
   "template": "refund_includes_tax+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0060",
   "template": "merge_mutates_base+shallow_merge+precedence_flipped"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0061",
   "template": "tax_per_mille+qty_plus_price+tax_on_prediscount"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0062",
   "template": "count_off_by_one+map_emit_reversed+once_shared_holder"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0063",
   "template": "tax_per_mille+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0064",
   "template": "shallow_merge+set_no_mkdir+precedence_flipped"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0065",
   "template": "once_shared_holder+unsubscribe_wrong_key+map_emit_reversed"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0066",
   "template": "unsubscribe_wrong_key+emit_stops_on_exception+once_shared_holder"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0067",
   "template": "boundary_exclusive+snapshot_shallow+precedence_flipped"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0068",
   "template": "emit_stops_on_exception+once_never_removes+once_shared_holder"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0069",
   "template": "precedence_flipped+snapshot_shallow+boundary_exclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0070",
   "template": "threshold_inclusive+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0071",
   "template": "set_no_mkdir+precedence_flipped+snapshot_shallow"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0072",
   "template": "unsubscribe_wrong_key+emit_stops_on_exception+count_off_by_one"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0073",
   "template": "snapshot_shallow+set_no_mkdir+shallow_merge"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0074",
   "template": "merge_mutates_base+snapshot_shallow+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0075",
   "template": "refund_includes_tax+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0076",
   "template": "tax_on_prediscount+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0077",
   "template": "emit_skips_first+count_off_by_one+emit_stops_on_exception"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 < v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0078",
   "template": "merge_mutates_base+snapshot_shallow+boundary_exclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0079",
   "template": "threshold_inclusive+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0080",
   "template": "snapshot_shallow+set_no_mkdir+precedence_flipped"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0081",
   "template": "qty_plus_price+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0082",
   "template": "precedence_flipped+boundary_exclusive+set_no_mkdir"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0083",
   "template": "precedence_flipped+boundary_exclusive+shallow_merge"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0084",
   "template": "map_emit_reversed+once_never_removes+emit_skips_first"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0085",
   "template": "merge_mutates_base+snapshot_shallow+precedence_flipped"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0086",
   "template": "bulk_rebate_threshold+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0087",
   "template": "map_emit_reversed+emit_skips_first+unsubscribe_wrong_key"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0088",
   "template": "snapshot_shallow+set_no_mkdir+boundary_exclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0089",
   "template": "qty_plus_price+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0090",
   "template": "once_never_removes+once_shared_holder+map_emit_reversed"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0091",
   "template": "refund_includes_tax+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0092",
   "template": "precedence_flipped+snapshot_shallow+shallow_merge"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0093",
   "template": "precedence_flipped+merge_mutates_base+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0094",
   "template": "threshold_inclusive+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0095",
   "template": "qty_plus_price+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0096",
   "template": "refund_includes_tax+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0097",
   "template": "qty_plus_price+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0098",
   "template": "refund_includes_tax+tax_per_mille+bulk_rebate_threshold"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0099",
   "template": "once_shared_holder+emit_stops_on_exception+count_off_by_one"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0100",
   "template": "snapshot_shallow+merge_mutates_base+shallow_merge"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0101",
   "template": "qty_plus_price+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0102",
   "template": "precedence_flipped+snapshot_shallow+merge_mutates_base"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0103",
   "template": "tax_per_mille+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0104",
   "template": "snapshot_shallow+boundary_exclusive+merge_mutates_base"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0105",
   "template": "refund_includes_tax+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0106",
   "template": "emit_stops_on_exception+once_never_removes+count_off_by_one"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0107",
   "template": "precedence_flipped+merge_mutates_base+boundary_exclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0108",
   "template": "tax_per_mille+threshold_inclusive+bulk_rebate_threshold"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0109",
   "template": "unsubscribe_wrong_key+once_never_removes+emit_stops_on_exception"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0110",
   "template": "tax_per_mille+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0111",
   "template": "qty_plus_price+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0112",
   "template": "map_emit_reversed+once_never_removes+emit_stops_on_exception"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0113",
   "template": "qty_plus_price+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0114",
   "template": "tax_per_mille+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0115",
   "template": "bulk_rebate_threshold+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        unsubscribe(_ONCE_HOLDER[\"sid\"])\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0116",
   "template": "emit_skips_first+once_shared_holder+unsubscribe_wrong_key"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 < v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0117",
   "template": "boundary_exclusive+merge_mutates_base+shallow_merge"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 < v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0118",
   "template": "shallow_merge+boundary_exclusive+snapshot_shallow"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0119",
   "template": "threshold_inclusive+qty_plus_price+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0120",
   "template": "tax_per_mille+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0121",
   "template": "refund_includes_tax+bulk_rebate_threshold+tax_on_prediscount"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0122",
   "template": "map_emit_reversed+emit_stops_on_exception+count_off_by_one"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0123",
   "template": "merge_mutates_base+set_no_mkdir+snapshot_shallow"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0124",
   "template": "bulk_rebate_threshold+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0125",
   "template": "tax_per_mille+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0126",
   "template": "boundary_exclusive+shallow_merge+snapshot_shallow"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0127",
   "template": "threshold_inclusive+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0128",
   "template": "boundary_exclusive+snapshot_shallow+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0129",
   "template": "refund_includes_tax+threshold_inclusive+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 < v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0130",
   "template": "precedence_flipped+boundary_exclusive+snapshot_shallow"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0131",
   "template": "refund_includes_tax+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0132",
   "template": "shallow_merge+merge_mutates_base+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0133",
   "template": "refund_includes_tax+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 < v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0134",
   "template": "snapshot_shallow+boundary_exclusive+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0135",
   "template": "refund_includes_tax+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0136",
   "template": "precedence_flipped+set_no_mkdir+snapshot_shallow"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0137",
   "template": "qty_plus_price+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 < v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0138",
   "template": "boundary_exclusive+precedence_flipped+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0139",
   "template": "bulk_rebate_threshold+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0140",
   "template": "tax_on_prediscount+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0141",
   "template": "tax_on_prediscount+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0142",
   "template": "tax_on_prediscount+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0143",
   "template": "bulk_rebate_threshold+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0144",
   "template": "bulk_rebate_threshold+threshold_inclusive+refund_includes_tax"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0145",
   "template": "set_no_mkdir+precedence_flipped+merge_mutates_base"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0146",
   "template": "tax_on_prediscount+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0147",
   "template": "threshold_inclusive+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0148",
   "template": "precedence_flipped+shallow_merge+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0149",
   "template": "refund_includes_tax+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0150",
   "template": "tax_per_mille+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0151",
   "template": "emit_skips_first+emit_stops_on_exception+map_emit_reversed"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 < v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0152",
   "template": "precedence_flipped+set_no_mkdir+boundary_exclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0153",
   "template": "bulk_rebate_threshold+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0154",
   "template": "refund_includes_tax+tax_on_prediscount+threshold_inclusive"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0155",
   "template": "emit_skips_first+once_never_removes+once_shared_holder"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0156",
   "template": "tax_per_mille+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0157",
   "template": "threshold_inclusive+qty_plus_price+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0158",
   "template": "tax_per_mille+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(reversed(_bus[\"handlers\"].get(event, [])))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0159",
   "template": "map_emit_reversed+count_off_by_one+emit_skips_first"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0160",
   "template": "tax_per_mille+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0161",
   "template": "refund_includes_tax+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0162",
   "template": "threshold_inclusive+qty_plus_price+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0163",
   "template": "threshold_inclusive+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0164",
   "template": "threshold_inclusive+qty_plus_price+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0165",
   "template": "refund_includes_tax+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0166",
   "template": "tax_per_mille+bulk_rebate_threshold+tax_on_prediscount"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 < v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0167",
   "template": "precedence_flipped+shallow_merge+boundary_exclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0168",
   "template": "tax_per_mille+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0169",
   "template": "tax_on_prediscount+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0170",
   "template": "tax_per_mille+bulk_rebate_threshold+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0171",
   "template": "threshold_inclusive+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0172",
   "template": "tax_per_mille+tax_on_prediscount+threshold_inclusive"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0173",
   "template": "snapshot_shallow+merge_mutates_base+shallow_merge"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0174",
   "template": "tax_per_mille+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 < v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0175",
   "template": "merge_mutates_base+boundary_exclusive+set_no_mkdir"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n_ONCE_HOLDER = {}\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    def wrapper(*args):\n        handler(*args)\n    _ONCE_HOLDER[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, [])) + 1\n",
   "task_id": "pb3-inte-0176",
   "template": "once_never_removes+count_off_by_one+once_shared_holder"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0177",
   "template": "refund_includes_tax+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0178",
   "template": "threshold_inclusive+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0179",
   "template": "refund_includes_tax+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0180",
   "template": "snapshot_shallow+merge_mutates_base+shallow_merge"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0181",
   "template": "threshold_inclusive+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0182",
   "template": "refund_includes_tax+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0183",
   "template": "tax_per_mille+threshold_inclusive+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0184",
   "template": "bulk_rebate_threshold+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0185",
   "template": "threshold_inclusive+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "interface",
   "hidden_tests": "import target\n\ndef run():\n    target.reset()\n    calls = []\n    a = target.subscribe(\"e\", lambda x: calls.append((\"a\", x)))\n    b = target.subscribe(\"e\", lambda x: calls.append((\"b\", x)))\n    target.emit(\"e\", 1)\n    assert calls == [(\"a\", 1), (\"b\", 1)], f\"emit order {calls}\"\n    assert target.handler_count(\"e\") == 2\n    assert target.unsubscribe(a) is True\n    target.emit(\"e\", 2)\n    assert calls == [(\"a\", 1), (\"b\", 1), (\"b\", 2)], f\"after unsub {calls}\"\n    assert target.handler_count(\"e\") == 1\n    assert target.unsubscribe(99999) is False\n    # raising handler does not stop the rest\n    target.reset()\n    seen = []\n    def boom(x): raise RuntimeError(\"x\")\n    target.subscribe(\"r\", boom)\n    target.subscribe(\"r\", lambda x: seen.append(x))\n    target.emit(\"r\", 5)\n    assert seen == [5], f\"emit continued {seen}\"\n    # once fires exactly once even with two registrations\n    target.reset()\n    c1, c2 = [], []\n    target.once(\"ev\", lambda v: c1.append(v))\n    target.once(\"ev\", lambda v: c2.append(v))\n    target.emit(\"ev\", \"x\")\n    target.emit(\"ev\", \"y\")\n    assert c1 == [\"x\"] and c2 == [\"x\"], f\"once {c1} {c2}\"\n    assert target.handler_count(\"ev\") == 0\n    # map_emit collects in order\n    target.reset()\n    target.subscribe(\"m\", lambda x: x + 1)\n    target.subscribe(\"m\", lambda x: x * 10)\n    assert target.map_emit(\"m\", 2) == [3, 20], \"map_emit\"\n    target.clear(\"m\")\n    assert target.handler_count(\"m\") == 0\n    target.emit(\"m\", 2)  # no handlers, no crash\n",
   "reference_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, [])):\n        try:\n            handler(*args)\n        except Exception:\n            continue\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if sid == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "statement": "Fix the event bus so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Tiny event bus.\n\nsubscribe(event, handler): register handler; returns an id.\nemit(event, *args): call every handler registered for event, in order.\n  A handler that raises does not prevent the remaining handlers.\nunsubscribe(sub_id): remove that registration; unknown ids are ignored.\nonce(event, handler): handler fires on the next emit only.\nmap_emit(event, *args): like emit, but return the list of handler results.\nclear(event): remove all registrations for event.\nhandler_count(event): number of live registrations for event.\n\"\"\"\n\ndef _new_bus():\n    return {\"handlers\": {}, \"seq\": 0}\n\n_bus = _new_bus()\n\ndef reset():\n    global _bus\n    _bus = _new_bus()\n\ndef subscribe(event, handler):\n    _bus[\"seq\"] += 1\n    sid = _bus[\"seq\"]\n    _bus[\"handlers\"].setdefault(event, []).append((sid, handler))\n    return sid\n\ndef emit(event, *args):\n    for sid, handler in list(_bus[\"handlers\"].get(event, []))[1:]:\n        try:\n            handler(*args)\n        except Exception:\n            break\n\ndef map_emit(event, *args):\n    return [handler(*args) for sid, handler in list(_bus[\"handlers\"].get(event, []))]\n\ndef clear(event):\n    _bus[\"handlers\"][event] = []\n\ndef unsubscribe(sub_id):\n    for event, regs in _bus[\"handlers\"].items():\n        for i, (sid, handler) in enumerate(regs):\n            if handler == sub_id:\n                del regs[i]\n                return True\n    return False\n\ndef once(event, handler):\n    holder = {}\n    def wrapper(*args):\n        unsubscribe(holder[\"sid\"])\n        handler(*args)\n    holder[\"sid\"] = subscribe(event, wrapper)\n\ndef handler_count(event):\n    return len(_bus[\"handlers\"].get(event, []))\n",
   "task_id": "pb3-inte-0186",
   "template": "emit_skips_first+emit_stops_on_exception+unsubscribe_wrong_key"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0187",
   "template": "bulk_rebate_threshold+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0188",
   "template": "qty_plus_price+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0189",
   "template": "threshold_inclusive+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0190",
   "template": "qty_plus_price+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0191",
   "template": "qty_plus_price+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0192",
   "template": "threshold_inclusive+bulk_rebate_threshold+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0193",
   "template": "qty_plus_price+tax_per_mille+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0194",
   "template": "tax_per_mille+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0195",
   "template": "tax_per_mille+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0196",
   "template": "tax_on_prediscount+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0197",
   "template": "threshold_inclusive+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0198",
   "template": "threshold_inclusive+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0199",
   "template": "qty_plus_price+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0200",
   "template": "tax_on_prediscount+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0201",
   "template": "bulk_rebate_threshold+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0202",
   "template": "tax_per_mille+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 < v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0203",
   "template": "boundary_exclusive+snapshot_shallow+merge_mutates_base"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0204",
   "template": "refund_includes_tax+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0205",
   "template": "qty_plus_price+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0206",
   "template": "refund_includes_tax+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0207",
   "template": "tax_on_prediscount+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0208",
   "template": "bulk_rebate_threshold+tax_on_prediscount+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0209",
   "template": "tax_on_prediscount+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0210",
   "template": "refund_includes_tax+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0211",
   "template": "tax_per_mille+threshold_inclusive+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0212",
   "template": "qty_plus_price+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0213",
   "template": "tax_per_mille+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0214",
   "template": "shallow_merge+snapshot_shallow+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0215",
   "template": "qty_plus_price+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0216",
   "template": "qty_plus_price+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0217",
   "template": "threshold_inclusive+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0218",
   "template": "tax_on_prediscount+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0219",
   "template": "precedence_flipped+snapshot_shallow+merge_mutates_base"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0220",
   "template": "qty_plus_price+threshold_inclusive+refund_includes_tax"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0221",
   "template": "set_no_mkdir+snapshot_shallow+shallow_merge"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0222",
   "template": "refund_includes_tax+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0223",
   "template": "threshold_inclusive+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0224",
   "template": "bulk_rebate_threshold+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0225",
   "template": "tax_on_prediscount+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 100, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 0, \"burst\": 100}}) is True\n    assert target.validate({\"limits\": {\"rate\": -1}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 101}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 0 <= v <= 100.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (0 <= v <= 100):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 0, \"burst\": 100}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0226",
   "template": "merge_mutates_base+shallow_merge+set_no_mkdir"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0227",
   "template": "tax_on_prediscount+bulk_rebate_threshold+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0228",
   "template": "bulk_rebate_threshold+tax_on_prediscount+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0229",
   "template": "bulk_rebate_threshold+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0230",
   "template": "refund_includes_tax+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0231",
   "template": "bulk_rebate_threshold+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0232",
   "template": "tax_on_prediscount+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0233",
   "template": "bulk_rebate_threshold+qty_plus_price+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0234",
   "template": "tax_per_mille+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0235",
   "template": "tax_on_prediscount+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0236",
   "template": "tax_on_prediscount+qty_plus_price+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0237",
   "template": "refund_includes_tax+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0238",
   "template": "threshold_inclusive+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0239",
   "template": "qty_plus_price+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0240",
   "template": "tax_on_prediscount+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0241",
   "template": "bulk_rebate_threshold+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0242",
   "template": "bulk_rebate_threshold+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0243",
   "template": "qty_plus_price+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 10, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 1, \"burst\": 10}}) is True\n    assert target.validate({\"limits\": {\"rate\": 0}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 11}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 1 <= v <= 10.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(env, file_cfg)\n    merged = merge(merged, _DEFAULTS)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (1 <= v <= 10):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = v\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 1, \"burst\": 10}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0244",
   "template": "precedence_flipped+set_no_mkdir+shallow_merge"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0245",
   "template": "tax_on_prediscount+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0246",
   "template": "tax_per_mille+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0247",
   "template": "tax_per_mille+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0248",
   "template": "refund_includes_tax+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0249",
   "template": "threshold_inclusive+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0250",
   "template": "bulk_rebate_threshold+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0251",
   "template": "threshold_inclusive+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0252",
   "template": "tax_on_prediscount+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0253",
   "template": "bulk_rebate_threshold+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0254",
   "template": "bulk_rebate_threshold+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0255",
   "template": "threshold_inclusive+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0256",
   "template": "bulk_rebate_threshold+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0257",
   "template": "tax_per_mille+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0258",
   "template": "tax_per_mille+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0259",
   "template": "refund_includes_tax+qty_plus_price+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0260",
   "template": "threshold_inclusive+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0261",
   "template": "bulk_rebate_threshold+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0262",
   "template": "bulk_rebate_threshold+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0263",
   "template": "threshold_inclusive+tax_per_mille+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0264",
   "template": "threshold_inclusive+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0265",
   "template": "bulk_rebate_threshold+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0266",
   "template": "threshold_inclusive+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0267",
   "template": "qty_plus_price+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0268",
   "template": "bulk_rebate_threshold+qty_plus_price+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0269",
   "template": "tax_per_mille+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0270",
   "template": "qty_plus_price+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0271",
   "template": "qty_plus_price+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0272",
   "template": "bulk_rebate_threshold+qty_plus_price+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0273",
   "template": "threshold_inclusive+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0274",
   "template": "bulk_rebate_threshold+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0275",
   "template": "qty_plus_price+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0276",
   "template": "bulk_rebate_threshold+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0277",
   "template": "tax_on_prediscount+qty_plus_price+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0278",
   "template": "tax_per_mille+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0279",
   "template": "tax_per_mille+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0280",
   "template": "tax_on_prediscount+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0281",
   "template": "threshold_inclusive+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0282",
   "template": "tax_on_prediscount+threshold_inclusive+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0283",
   "template": "refund_includes_tax+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0284",
   "template": "refund_includes_tax+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0285",
   "template": "threshold_inclusive+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0286",
   "template": "qty_plus_price+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0287",
   "template": "bulk_rebate_threshold+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0288",
   "template": "threshold_inclusive+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0289",
   "template": "threshold_inclusive+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0290",
   "template": "qty_plus_price+bulk_rebate_threshold+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0291",
   "template": "refund_includes_tax+threshold_inclusive+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0292",
   "template": "qty_plus_price+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0293",
   "template": "threshold_inclusive+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0294",
   "template": "tax_on_prediscount+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0295",
   "template": "threshold_inclusive+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0296",
   "template": "tax_per_mille+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0297",
   "template": "threshold_inclusive+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0298",
   "template": "tax_per_mille+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0299",
   "template": "tax_per_mille+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0300",
   "template": "qty_plus_price+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0301",
   "template": "bulk_rebate_threshold+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0302",
   "template": "bulk_rebate_threshold+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0303",
   "template": "bulk_rebate_threshold+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0304",
   "template": "refund_includes_tax+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0305",
   "template": "refund_includes_tax+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0306",
   "template": "bulk_rebate_threshold+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0307",
   "template": "threshold_inclusive+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0308",
   "template": "bulk_rebate_threshold+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0309",
   "template": "refund_includes_tax+tax_on_prediscount+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0310",
   "template": "tax_on_prediscount+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0311",
   "template": "refund_includes_tax+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0312",
   "template": "tax_on_prediscount+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0313",
   "template": "threshold_inclusive+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0314",
   "template": "qty_plus_price+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0315",
   "template": "bulk_rebate_threshold+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0316",
   "template": "refund_includes_tax+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0317",
   "template": "tax_on_prediscount+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0318",
   "template": "tax_on_prediscount+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0319",
   "template": "tax_on_prediscount+threshold_inclusive+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0320",
   "template": "tax_per_mille+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0321",
   "template": "tax_per_mille+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0322",
   "template": "bulk_rebate_threshold+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0323",
   "template": "threshold_inclusive+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0324",
   "template": "qty_plus_price+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0325",
   "template": "bulk_rebate_threshold+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0326",
   "template": "tax_on_prediscount+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0327",
   "template": "tax_per_mille+bulk_rebate_threshold+tax_on_prediscount"
  },
  {
   "family": "config",
   "hidden_tests": "import target\n\ndef run():\n    target.load({\"limits\": {\"rate\": 5}}, {\"db\": {\"host\": \"prod\"}})\n    assert target.get(\"db.host\") == \"prod\", \"env wins\"\n    assert target.get(\"limits.rate\") == 5, \"file value kept\"\n    assert target.get(\"limits.burst\") == 50, \"default kept\"\n    assert target.get(\"nope\", \"dflt\") == \"dflt\", \"default\"\n    target.set(\"new.deep.key\", 7)\n    assert target.get(\"new.deep.key\") == 7, \"dotted set\"\n    assert target.validate({\"limits\": {\"rate\": 5, \"burst\": 50}}) is True\n    assert target.validate({\"limits\": {\"rate\": 4}}) is False, \"below lo\"\n    assert target.validate({\"limits\": {\"burst\": 51}}) is False, \"above hi\"\n    base = {\"a\": {\"x\": 1}}\n    m = target.merge(base, {\"a\": {\"y\": 2}})\n    assert m == {\"a\": {\"x\": 1, \"y\": 2}}, f\"deep merge {m}\"\n    assert base == {\"a\": {\"x\": 1}}, \"merge must not mutate base\"\n    target.load({}, {})\n    s = target.snapshot()\n    target.set(\"limits.rate\", 999)\n    target.restore(s)\n    assert target.get(\"limits.rate\") != 999, \"snapshot/restore nested\"\n",
   "reference_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur.setdefault(part, {})\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = dict(base)\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return copy.deepcopy(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "statement": "Fix the layered settings module so every function matches its docstring. Later layers win. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Layered settings.\n\nDefaults < file config < environment overrides (later wins).\nget(key, default=None): dotted lookup, e.g. \"db.host\".\nset(key, value): dotted set, creating intermediate dicts.\nload(file_cfg, env): build the effective settings dict.\nvalidate(cfg): every \"limits.*\" value must satisfy 5 <= v <= 50.\nmerge(base, override): deep merge; override wins; inputs not mutated.\nsnapshot(): deep copy of the effective settings.\nrestore(snap): replace effective settings with the snapshot.\n\"\"\"\n\nimport copy\n\ndef _get_path(cfg, key, default=None):\n    cur = cfg\n    for part in key.split(\".\"):\n        if not isinstance(cur, dict) or part not in cur:\n            return default\n        cur = cur[part]\n    return cur\n\ndef get(key, default=None):\n    return _get_path(_EFFECTIVE, key, default)\n\ndef set(key, value):\n    parts = key.split(\".\")\n    cur = _EFFECTIVE\n    for part in parts[:-1]:\n        cur = cur[part]\n    cur[parts[-1]] = value\n\ndef load(file_cfg, env):\n    global _EFFECTIVE\n    merged = merge(_DEFAULTS, file_cfg)\n    merged = merge(merged, env)\n    _EFFECTIVE = merged\n    return merged\n\ndef validate(cfg):\n    for key in (\"limits.rate\", \"limits.burst\"):\n        v = _get_path(cfg, key)\n        if v is None:\n            continue\n        if not (5 <= v <= 50):\n            return False\n    return True\n\ndef merge(base, override):\n    out = base\n    for k, v in override.items():\n        if k in out and isinstance(out[k], dict) and isinstance(v, dict):\n            out[k] = merge(out[k], v)\n        else:\n            out[k] = v\n    return out\n\ndef snapshot():\n    return dict(_EFFECTIVE)\n\ndef restore(snap):\n    global _EFFECTIVE\n    _EFFECTIVE = copy.deepcopy(snap)\n\n_DEFAULTS = {\"limits\": {\"rate\": 5, \"burst\": 50}, \"db\": {\"host\": \"localhost\"}}\n_EFFECTIVE = copy.deepcopy(_DEFAULTS)\n",
   "task_id": "pb3-conf-0328",
   "template": "set_no_mkdir+snapshot_shallow+merge_mutates_base"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0329",
   "template": "bulk_rebate_threshold+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0330",
   "template": "tax_per_mille+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0331",
   "template": "tax_on_prediscount+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0332",
   "template": "threshold_inclusive+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0333",
   "template": "qty_plus_price+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0334",
   "template": "bulk_rebate_threshold+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0335",
   "template": "bulk_rebate_threshold+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0336",
   "template": "tax_per_mille+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0337",
   "template": "bulk_rebate_threshold+qty_plus_price+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0338",
   "template": "bulk_rebate_threshold+tax_on_prediscount+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0339",
   "template": "refund_includes_tax+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0340",
   "template": "bulk_rebate_threshold+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0341",
   "template": "bulk_rebate_threshold+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0342",
   "template": "threshold_inclusive+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0343",
   "template": "refund_includes_tax+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0344",
   "template": "threshold_inclusive+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0345",
   "template": "refund_includes_tax+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0346",
   "template": "refund_includes_tax+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0347",
   "template": "bulk_rebate_threshold+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0348",
   "template": "qty_plus_price+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0349",
   "template": "qty_plus_price+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0350",
   "template": "qty_plus_price+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0351",
   "template": "refund_includes_tax+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0352",
   "template": "refund_includes_tax+qty_plus_price+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0353",
   "template": "bulk_rebate_threshold+tax_on_prediscount+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0354",
   "template": "tax_on_prediscount+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0355",
   "template": "bulk_rebate_threshold+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0356",
   "template": "threshold_inclusive+bulk_rebate_threshold+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0357",
   "template": "threshold_inclusive+qty_plus_price+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0358",
   "template": "refund_includes_tax+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0359",
   "template": "qty_plus_price+threshold_inclusive+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0360",
   "template": "bulk_rebate_threshold+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0361",
   "template": "threshold_inclusive+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0362",
   "template": "tax_per_mille+qty_plus_price+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0363",
   "template": "threshold_inclusive+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0364",
   "template": "threshold_inclusive+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0365",
   "template": "tax_per_mille+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0366",
   "template": "tax_per_mille+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0367",
   "template": "refund_includes_tax+tax_per_mille+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0368",
   "template": "qty_plus_price+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0369",
   "template": "tax_on_prediscount+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0370",
   "template": "threshold_inclusive+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0371",
   "template": "refund_includes_tax+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0372",
   "template": "refund_includes_tax+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0373",
   "template": "tax_per_mille+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0374",
   "template": "threshold_inclusive+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0375",
   "template": "bulk_rebate_threshold+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0376",
   "template": "refund_includes_tax+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0377",
   "template": "refund_includes_tax+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0378",
   "template": "tax_per_mille+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0379",
   "template": "qty_plus_price+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0380",
   "template": "qty_plus_price+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0381",
   "template": "tax_per_mille+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0382",
   "template": "refund_includes_tax+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0383",
   "template": "bulk_rebate_threshold+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0384",
   "template": "threshold_inclusive+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0385",
   "template": "bulk_rebate_threshold+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0386",
   "template": "threshold_inclusive+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0387",
   "template": "bulk_rebate_threshold+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0388",
   "template": "tax_per_mille+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0389",
   "template": "tax_per_mille+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0390",
   "template": "threshold_inclusive+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0391",
   "template": "tax_per_mille+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0392",
   "template": "threshold_inclusive+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0393",
   "template": "threshold_inclusive+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0394",
   "template": "tax_on_prediscount+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0395",
   "template": "tax_on_prediscount+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0396",
   "template": "bulk_rebate_threshold+threshold_inclusive+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0397",
   "template": "tax_per_mille+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0398",
   "template": "tax_per_mille+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0399",
   "template": "threshold_inclusive+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0400",
   "template": "refund_includes_tax+bulk_rebate_threshold+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0401",
   "template": "threshold_inclusive+bulk_rebate_threshold+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0402",
   "template": "threshold_inclusive+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0403",
   "template": "tax_on_prediscount+bulk_rebate_threshold+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0404",
   "template": "bulk_rebate_threshold+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0405",
   "template": "refund_includes_tax+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0406",
   "template": "tax_on_prediscount+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0407",
   "template": "bulk_rebate_threshold+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0408",
   "template": "tax_per_mille+bulk_rebate_threshold+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0409",
   "template": "qty_plus_price+tax_on_prediscount+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0410",
   "template": "tax_on_prediscount+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0411",
   "template": "tax_per_mille+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0412",
   "template": "bulk_rebate_threshold+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0413",
   "template": "tax_per_mille+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0414",
   "template": "tax_on_prediscount+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0415",
   "template": "refund_includes_tax+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0416",
   "template": "tax_per_mille+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0417",
   "template": "qty_plus_price+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0418",
   "template": "refund_includes_tax+bulk_rebate_threshold+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0419",
   "template": "bulk_rebate_threshold+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0420",
   "template": "threshold_inclusive+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0421",
   "template": "refund_includes_tax+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0422",
   "template": "refund_includes_tax+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0423",
   "template": "threshold_inclusive+bulk_rebate_threshold+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0424",
   "template": "tax_on_prediscount+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0425",
   "template": "tax_per_mille+qty_plus_price+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0426",
   "template": "tax_per_mille+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0427",
   "template": "threshold_inclusive+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0428",
   "template": "tax_per_mille+bulk_rebate_threshold+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0429",
   "template": "bulk_rebate_threshold+tax_on_prediscount+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0430",
   "template": "refund_includes_tax+threshold_inclusive+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0431",
   "template": "tax_per_mille+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0432",
   "template": "tax_per_mille+refund_includes_tax+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0433",
   "template": "refund_includes_tax+tax_per_mille+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0434",
   "template": "tax_on_prediscount+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0435",
   "template": "refund_includes_tax+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0436",
   "template": "refund_includes_tax+threshold_inclusive+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0437",
   "template": "qty_plus_price+tax_on_prediscount+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0438",
   "template": "threshold_inclusive+bulk_rebate_threshold+tax_on_prediscount"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0439",
   "template": "tax_on_prediscount+tax_per_mille+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0440",
   "template": "tax_per_mille+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0441",
   "template": "tax_per_mille+bulk_rebate_threshold+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0442",
   "template": "threshold_inclusive+refund_includes_tax+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0443",
   "template": "tax_per_mille+threshold_inclusive+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0444",
   "template": "bulk_rebate_threshold+threshold_inclusive+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0445",
   "template": "refund_includes_tax+bulk_rebate_threshold+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0446",
   "template": "tax_on_prediscount+bulk_rebate_threshold+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0447",
   "template": "refund_includes_tax+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0448",
   "template": "qty_plus_price+tax_on_prediscount+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0449",
   "template": "tax_per_mille+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0450",
   "template": "qty_plus_price+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0451",
   "template": "tax_on_prediscount+tax_per_mille+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0452",
   "template": "qty_plus_price+refund_includes_tax+tax_per_mille"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0453",
   "template": "bulk_rebate_threshold+threshold_inclusive+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0454",
   "template": "bulk_rebate_threshold+threshold_inclusive+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0455",
   "template": "tax_on_prediscount+tax_per_mille+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(200) == 200, \"at threshold no discount\"\n    assert target.apply_discount(201) == round((201) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 200.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 200:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0456",
   "template": "qty_plus_price+tax_on_prediscount+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0457",
   "template": "qty_plus_price+refund_includes_tax+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0458",
   "template": "refund_includes_tax+tax_per_mille+threshold_inclusive"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n > 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0459",
   "template": "tax_per_mille+qty_plus_price+bulk_rebate_threshold"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 15/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 15% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 500:\n        return round(subtotal * (1 - 15 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0460",
   "template": "threshold_inclusive+refund_includes_tax+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 12 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 12% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 12 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0461",
   "template": "tax_on_prediscount+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 8 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 8% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 8 / 1000, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0462",
   "template": "tax_per_mille+tax_on_prediscount+qty_plus_price"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(500) == 500, \"at threshold no discount\"\n    assert target.apply_discount(501) == round((501) * (1 - 20/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 5 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 20% off when subtotal is strictly greater than 500.\ncompute_tax(amount): 5% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] + it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 500:\n        return round(subtotal * (1 - 20 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 5 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0463",
   "template": "tax_on_prediscount+qty_plus_price+refund_includes_tax"
  },
  {
   "family": "logic",
   "hidden_tests": "import target\n\ndef run():\n    items = [{\"name\": \"a\", \"qty\": 2, \"price\": 60.0}, {\"name\": \"b\", \"qty\": 1, \"price\": 30.0}]\n    assert target.compute_subtotal(items) == 150.0, \"subtotal\"\n    assert target.apply_discount(100) == 100, \"at threshold no discount\"\n    assert target.apply_discount(101) == round((101) * (1 - 10/100), 2), \"discount\"\n    assert target.compute_tax(200.0) == round(200.0 * 10 / 100, 2), \"tax\"\n    d = target.apply_discount(150.0)\n    assert target.order_total(items) == round(d + target.compute_tax(d), 2), \"order_total\"\n    # discount-before-tax matters: use a subtotal above every threshold\n    big = [{\"name\": \"a\", \"qty\": 20, \"price\": 60.0}]\n    subb = target.compute_subtotal(big)\n    assert subb == 1200.0, \"big subtotal\"\n    db = target.apply_discount(subb)\n    assert target.order_total(big) == round(db + target.compute_tax(db), 2), \"order_total big\"\n    assert target.bulk_quote(items, 9) == round(target.order_total(items) * 9, 2), \"bulk no rebate\"\n    assert target.bulk_quote(items, 10) == round(target.order_total(items) * 10 * 0.95, 2), \"bulk rebate at 10\"\n    assert target.refund(items, [\"a\"]) == 120.0, \"refund pre-tax\"\n    r = target.format_receipt(items)\n    assert f\"TOTAL: {target.order_total(items)}\" in r, \"receipt\"\n",
   "reference_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal > 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(d), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base, 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "statement": "Fix the order-processing module so every function matches its docstring. There may be several defects, including subtle ones.",
   "support_files": {},
   "target_code": "\"\"\"Order processing module.\n\ncompute_subtotal(items): sum of qty*price over items.\napply_discount(subtotal): 10% off when subtotal is strictly greater than 100.\ncompute_tax(amount): 10% sales tax on the amount.\norder_total(items): discount first, then tax on the discounted amount.\nbulk_quote(items, n): n identical orders; 5% volume rebate when n >= 10,\n  applied to the order total.\nrefund(items, returned_names): refund the subtotal (pre-discount, pre-tax)\n  of the returned line items.\nformat_receipt(items): multi-line receipt string; totals must match the\nfunctions above.\n\"\"\"\n\ndef compute_subtotal(items):\n    total = 0.0\n    for it in items:\n        total += it[\"qty\"] * it[\"price\"]\n    return round(total, 2)\n\ndef apply_discount(subtotal):\n    if subtotal >= 100:\n        return round(subtotal * (1 - 10 / 100), 2)\n    return subtotal\n\ndef compute_tax(amount):\n    return round(amount * 10 / 100, 2)\n\ndef order_total(items):\n    sub = compute_subtotal(items)\n    d = apply_discount(sub)\n    return round(d + compute_tax(sub), 2)\n\ndef bulk_quote(items, n):\n    t = order_total(items) * n\n    if n >= 10:\n        t = t * 0.95\n    return round(t, 2)\n\ndef refund(items, returned_names):\n    base = sum(it[\"qty\"] * it[\"price\"] for it in items\n               if it[\"name\"] in returned_names)\n    return round(base + compute_tax(base), 2)\n\ndef format_receipt(items):\n    lines = []\n    for it in items:\n        lines.append(f\"{it['name']} x{it['qty']} @ {it['price']}\")\n    lines.append(f\"TOTAL: {order_total(items)}\")\n    return \"\\n\".join(lines)\n",
   "task_id": "pb3-logi-0464",
   "template": "refund_includes_tax+tax_on_prediscount+threshold_inclusive"
  }
 ]
}