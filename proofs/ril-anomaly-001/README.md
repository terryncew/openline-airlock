# RIL-ANOMALY-001 terminal freeze

Formal verdict: `INCONCLUSIVE_RIL_ANOMALY_001_TERMINAL_FAILURE`

Terminal cause: `PROTOCOL_FAILURE_CROSS_ARM_PRODUCT_MEMORY_CONTAMINATION_BEFORE_CONTROL_ROUND_1`.

Session 1 completed four anomaly-interview rounds and produced two receiver-confirmed promotions after two quarantines. Those are preserved as single-arm observations only.

Before session 2 ordinary-search round 1 produced a response, Prince surfaced remembered state from session 1, including the promoted diversity-remainder lesson and successor head, and rejected the session-2 packet as stale. The packet was not stale: each arm was preregistered to start from the same initial arm-local accepted head. The surfaced treatment-arm memory therefore contaminated the matched control before it began.

The preregistration explicitly did not claim that persistent product memory was absent. Once cross-arm memory actually surfaced, however, the second arm could no longer differ from the first only by research protocol. Asking Prince to ignore the remembered result would not restore an unexposed control, so the experiment stops rather than rescues the comparison.

RIL-ANOMALY-001 therefore earns no matched-arm advantage claim. Any successor that wants a clean comparison needs a new experiment ID and a substrate that can demonstrate arm isolation or a design robust to crossover memory.
