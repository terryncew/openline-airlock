diff --git a/binary_ninja_headless_mcp/backend.py b/binary_ninja_headless_mcp/backend.py
--- a/binary_ninja_headless_mcp/backend.py
+++ b/binary_ninja_headless_mcp/backend.py
@@ -5,6 +5,7 @@ import base64
 import binascii
 import io
+import math
 import os
 import re
 import tempfile
@@ -12,6 +13,7 @@ from concurrent.futures import Future, ThreadPoolExecutor
 from contextlib import redirect_stderr, redirect_stdout, suppress
 from dataclasses import dataclass, field, fields, is_dataclass
+from pathlib import Path
 from types import ModuleType
 from typing import Any
 from uuid import uuid4
@@ -111,7 +113,7 @@ class BinjaBackend:
         if deterministic:
             self._apply_determinism_env(True)

-        suffix = f".{filename}" if filename else ".bin"
+        suffix = Path(filename).suffix or ".bin"
         with tempfile.NamedTemporaryFile(
             prefix="binary_ninja_headless_mcp-",
             suffix=suffix,
